[package]
name = "lau-ai-tutor"
version = "0.1.0"
edition = "2021"
description = "Intelligence layer for PLATO — an AI tutor that adapts to every kid."

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
